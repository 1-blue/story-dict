services:
  monorepo:
    build: 
      context: .
      dockerfile: Dockerfile.production
    container_name: monorepo
    ports:
      - "9000:9000"
      - "9050:9050"
    env_file:
      - ./envs/be/.env.production
      - ./envs/fe/.env.production