import type { NextPage } from "next";

const Page: NextPage = () => {
  return (
    <div className="flex flex-col items-center justify-center gap-4">
      <span>이스터에그 .. 🐣</span>
      <span>
        사실 어떤 컨텐츠에 어떤 레이아웃을 사용할지 미정이랍니다 .. 🥲
      </span>
    </div>
  );
};

export default Page;
